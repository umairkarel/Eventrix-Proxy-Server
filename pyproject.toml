[project]
name = "proxy-server"
version = "0.1.0"
description = "First-party GA4 proxy server"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "httpx>=0.27.0",
    "uvicorn[standard]>=0.29.0",
]

[tool.uv]
package = false
